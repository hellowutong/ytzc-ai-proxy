# AI Gateway Environment Configuration
# Copy this file to .env and update the values
#
# Usage:
#   cp .env.example .env
#   nano .env  # Edit values
#   docker-compose up -d

# ============================================
# INFRASTRUCTURE PORTS
# ============================================
# MongoDB Port
MONGODB_PORT=27017

# Redis Port
REDIS_PORT=6379

# Qdrant Vector DB Port
QDRANT_PORT=6333

# ============================================
# APPLICATION PORTS
# ============================================
# Backend API Port
APP_PORT=8000

# Frontend UI Port
UI_PORT=5175

# Nginx Port (production)
NGINX_PORT=80
NGINX_SSL_PORT=443

# SearXNG Port (optional)
SEARXNG_PORT=8080

# ============================================
# MongoDB Configuration
# ============================================
MONGODB_ROOT_USERNAME=admin
MONGODB_ROOT_PASSWORD=admin123  # Change in production!
MONGODB_DATABASE=ai_gateway
MONGODB_USERNAME=ai_gateway_user
MONGODB_PASSWORD=user123  # Change in production!

# ============================================
# Redis Configuration
# ============================================
REDIS_PASSWORD=redis123  # Change in production!

# ============================================
# Qdrant Configuration (Vector Database)
# ============================================
QDRANT_API_KEY=  # Set API key if Qdrant requires authentication
QDRANT_LOG_LEVEL=INFO

# ============================================
# Application Configuration
# ============================================
LOG_LEVEL=INFO
ENVIRONMENT=development  # Set to 'production' in production
DEBUG=false

# Workers (production only, recommended: CPU cores or 2-4)
WORKERS=1

# ============================================
# Frontend Configuration
# ============================================
NODE_ENV=development
VITE_API_BASE_URL=http://localhost:8000  # Change to your domain in production

# ============================================
# API Keys (Optional - for external services)
# ============================================
# SiliconFlow API (for embeddings and LLM calls)
# SILICONFLOW_API_KEY=sk-xxx
# SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# OpenAI API (alternative)
# OPENAI_API_KEY=sk-xxx

# ============================================
# Docker Configuration
# ============================================
# Docker Compose project name
COMPOSE_PROJECT_NAME=ai-gateway

# Restart policy (no, always, unless-stopped, on-failure)
RESTART_POLICY=unless-stopped

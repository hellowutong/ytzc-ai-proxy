# RSS文章删除功能 - 工作计划

## 任务清单

### 后端任务
1. [ ] 修改 `backend/core/rss_fetcher.py` - 添加 `delete_article()` 方法
2. [ ] 修改 `backend/core/rss_fetcher.py` - 添加 `delete_articles()` 方法
3. [ ] 修改 `backend/api/admin/v1/rss.py` - 添加删除单篇文章API端点
4. [ ] 修改 `backend/api/admin/v1/rss.py` - 添加批量删除文章API端点

### 前端任务
5. [ ] 创建 `frontend/src/components/ArticleContextMenu.vue` - 文章右键菜单组件
6. [ ] 修改 `frontend/src/stores/rss.ts` - 添加 `deleteArticle()` 方法
7. [ ] 修改 `frontend/src/stores/rss.ts` - 添加 `batchDeleteArticles()` 方法
8. [ ] 修改 `frontend/src/views/admin/RSS.vue` - 添加批量选择复选框
9. [ ] 修改 `frontend/src/views/admin/RSS.vue` - 添加批量删除按钮
10. [ ] 修改 `frontend/src/views/admin/RSS.vue` - 添加右键菜单事件处理
11. [ ] 修改 `frontend/src/views/admin/RSS.vue` - 添加快捷键支持(Delete/Ctrl+A)

### 测试验证
12. [ ] 后端API测试
13. [ ] 前端功能测试
14. [ ] 联调测试

## API规范

### 删除单篇文章
```
DELETE /admin/ai/v1/rss/articles/{id}

Response:
{
  "success": true,
  "message": "文章已删除",
  "article_id": "xxx"
}
```

### 批量删除文章
```
DELETE /admin/ai/v1/rss/articles/batch?ids=id1,id2,id3

Response:
{
  "code": 200,
  "message": "已删除 3 篇文章",
  "data": {
    "success_count": 3,
    "failed_count": 0,
    "failed_ids": []
  }
}
```

## 界面设计

### 文章条目结构
```
┌─────────────────────────────────────────┐
│ □  ● 文章标题 (复选框+未读圆点)           │
│      摘要预览文本，最多显示两行...        │
│      少数派 · 2小时前                     │
└─────────────────────────────────────────┘
```

### 右键菜单
```
┌──────────────┐
│ 📖 标记为已读  │
│ 📖 标记为未读  │
├──────────────┤
│ 🔗 复制链接    │
├──────────────┤
│ 🗑️ 删除文章    │ ← 红色
└──────────────┘
```

### 工具栏
```
[搜索框...] [全部▼] [□ 全选] [🗑️ 批量删除(0)] [🔄]
```
